<section>
  <link rel="stylesheet" href="/demo-sites/jetstream/preview-styles.css" />
  <div x-data="sourceEditorManager()" class="hidden max-w-screen-lg mx-auto md:block">

    <div
      id="source-demo-window"
      class="relative flex flex-row gap-3"
      style="height: 700px;">

      <!-- Left Panel: Code Editor -->
      <div class="relative flex flex-col w-1/2 overflow-hidden rounded-lg" style="border: 1px solid #313244;">
        {% bookshop "interactive-source-demo/bits/sidebar" %}
      </div>

      <!-- Right Panel: Visual Editor -->
      <div class="flex flex-col flex-1 overflow-hidden bg-white border-2 border-black rounded-lg shadow-lg">
        <div class="flex p-2 pl-4 space-x-2 border-b border-gray-200">
          <span class="w-2 h-2 bg-[#EA6053] rounded-full"></span>
          <span class="w-2 h-2 bg-[#F0B646] rounded-full"></span>
          <span class="w-2 h-2 bg-[#58BD4E] rounded-full"></span>
        </div>
        <div class="flex items-center justify-between px-3 py-1.5 border-b border-gray-200 bg-gray-50">
          <div class="flex items-center gap-2">
            <button class="p-1 rounded hover:bg-gray-200" aria-label="Back">
              <svg
                class="w-4 h-4 text-gray-400"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"><path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M15 19l-7-7 7-7" /></svg>
            </button>
            <div class="flex items-center gap-2">
              <div class="flex items-center justify-center rounded w-7 h-7" style="background: #2b2b2b;">
                <svg
                  class="w-4 h-4 text-white"
                  xmlns="http://www.w3.org/2000/svg"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"><path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" /></svg>
              </div>
              <div class="leading-tight">
                <div class="text-sm font-semibold" x-text="showDataView ? 'Blog Post' : 'Home'"></div>
                <div class="text-xs text-gray-400" x-text="showDataView ? '/blog/' + site.blogSidebarBottomFilename.replace('.md', '') : '/'"></div>
              </div>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <div class="flex items-center justify-center text-sm rounded-full w-7 h-7 bg-amber-100">🙂</div>
            <span
              class="text-xs"
              :class="hasChanges ? 'text-amber-600 font-semibold' : 'text-gray-400'"
              x-text="hasChanges ? 'Unsaved changes' : 'No pending changes'"></span>
            <div class="relative">
              <button
                class="save-btn text-xs font-semibold rounded-md transition-all"
                :class="hasChanges ? 'save-btn-active' : 'save-btn-inactive'"
                @click="showSaveModal = true">Save</button>
              <span
                x-show="hasChanges"
                x-transition:enter="transition ease-out duration-200"
                x-transition:enter-start="opacity-0 scale-0"
                x-transition:enter-end="opacity-100 scale-100"
                class="save-btn-badge">1</span>
            </div>
          </div>
        </div>
        {% bookshop "interactive-source-demo/bits/main" %}
      </div>

      <!-- Tutorial Tooltip -->
      <div
        x-show="activeTutorial !== null && tutorialStep <= maxTutorialStep"
        x-transition:enter="transition ease-out duration-200"
        x-transition:enter-start="opacity-0 scale-95"
        x-transition:enter-end="opacity-100 scale-100"
        x-transition:leave="transition ease-in duration-150"
        x-transition:leave-start="opacity-100 scale-100"
        x-transition:leave-end="opacity-0 scale-95"
        class="tutorial-tooltip"
        :style="activeTutorial !== null && tutorialStep <= maxTutorialStep ? 'top: ' + tooltipTop() + 'px' : ''">
        <div class="text-sm leading-snug text-gray-700" x-text="currentTooltipSteps[tutorialStep]?.text"></div>
        <div class="flex items-center justify-between mt-3">
          <span class="text-xs text-gray-400" x-text="(tutorialStep + 1) + ' / ' + currentTooltipSteps.length"></span>
          <button
            class="px-3 py-1 text-xs font-semibold text-white rounded"
            style="background-color: #034AD8;"
            @click="nextStep()"
            x-text="currentTooltipSteps[tutorialStep]?.button"></button>
        </div>
      </div>

      <!-- Image Picker -->
      <div
        x-show="showImagePicker"
        x-transition:enter="transition ease-out duration-200"
        x-transition:enter-start="opacity-0 scale-95"
        x-transition:enter-end="opacity-100 scale-100"
        x-transition:leave="transition ease-in duration-150"
        x-transition:leave-start="opacity-100 scale-100"
        x-transition:leave-end="opacity-0 scale-95"
        class="image-picker"
        @click.outside="showImagePicker = false; showImageSourceMenu = false; showGallery = false">

        <div class="image-picker-header">
          <div class="flex items-center gap-2">
            <svg
              class="w-4 h-4 text-gray-400"
              viewBox="0 0 16 16"
              fill="currentColor">
              <circle
                cx="4"
                cy="3"
                r="1.5" /><circle
                cx="4"
                cy="8"
                r="1.5" /><circle
                cx="4"
                cy="13"
                r="1.5" />
              <circle
                cx="10"
                cy="3"
                r="1.5" /><circle
                cx="10"
                cy="8"
                r="1.5" /><circle
                cx="10"
                cy="13"
                r="1.5" />
            </svg>
            <span class="text-sm font-bold text-gray-900">Image</span>
          </div>
          <button @click="showImagePicker = false" class="p-0.5 text-gray-400 rounded hover:text-gray-600 hover:bg-gray-100">
            <svg
              class="w-4 h-4"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>

        <div class="mb-4">
          <label class="block text-sm font-bold text-gray-900 mb-0.5">Image Path</label>
          <p class="text-xs text-gray-500 mb-2">The path to the source file of the image to be optimized.</p>
          <div class="image-picker-path-row">
            <input
              type="text"
              x-model="heroImageSrc"
              placeholder="/assets/images/example.jpg"
              @keydown.enter="applyPathInput()"
              @blur="applyPathInput()"
              class="image-picker-path-input" />
            <button
              @click="heroImageSrc = ''; heroImagePreviewSrc = ''; onEdit('heroImageSrc')"
              class="image-picker-path-btn"
              title="Clear">
              <svg
                class="w-4 h-4"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor">
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2M3 12l6.414-6.414a2 2 0 011.414-.586H19a2 2 0 012 2v10a2 2 0 01-2 2h-8.172a2 2 0 01-1.414-.586L3 12z" />
              </svg>
            </button>
            <div class="relative">
              <button
                @click="showImageSourceMenu = !showImageSourceMenu"
                class="image-picker-path-btn image-picker-add-btn"
                title="Add image">
                <svg
                  class="w-4 h-4"
                  xmlns="http://www.w3.org/2000/svg"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor">
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M12 4v16m8-8H4" />
                </svg>
              </button>
              <div
                x-show="showImageSourceMenu"
                x-transition:enter="transition ease-out duration-100"
                x-transition:enter-start="opacity-0 scale-95"
                x-transition:enter-end="opacity-100 scale-100"
                x-transition:leave="transition ease-in duration-75"
                x-transition:leave-start="opacity-100 scale-100"
                x-transition:leave-end="opacity-0 scale-95"
                @click.outside="showImageSourceMenu = false"
                class="image-picker-source-menu">
                <label class="image-picker-source-option">
                  <svg
                    class="w-5 h-5 text-gray-500"
                    xmlns="http://www.w3.org/2000/svg"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor">
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12" />
                  </svg>
                  <span>Upload a new image</span>
                  <input
                    type="file"
                    accept="image/*"
                    class="hidden"
                    @change="handleImageUpload($event)" />
                </label>
                <button @click="showGallery = !showGallery; showImageSourceMenu = false" class="image-picker-source-option">
                  <svg
                    class="w-5 h-5 text-gray-500"
                    xmlns="http://www.w3.org/2000/svg"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor">
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z" />
                  </svg>
                  <span>Select existing image</span>
                </button>
              </div>
            </div>
          </div>
        </div>

        <div
          x-show="showGallery"
          x-transition
          class="mb-4">
          <div class="image-picker-grid">
            <template x-for="preset in site.presetImages" :key="preset.previewSrc">
              <button
                class="image-picker-thumb"
                :class="heroImagePreviewSrc === preset.previewSrc && 'selected'"
                @click="applyPresetImage(preset)">
                <img :src="preset.previewSrc" alt="" />
              </button>
            </template>
          </div>
        </div>

        <div
          class="image-picker-preview"
          x-show="heroImagePreviewSrc"
          x-transition>
          <img :src="heroImagePreviewSrc" alt="" />
        </div>

        <div>
          <label class="block text-sm font-bold text-gray-900 mb-1">Image Alt Text</label>
          <textarea
            x-model="heroImageAlt"
            @input="onEdit('heroImageAlt')"
            rows="3"
            class="image-picker-alt-input"
            placeholder="Describe the image..."></textarea>
        </div>
      </div>

      <!-- Save Modal -->
      <div
        x-show="showSaveModal"
        x-transition:enter="transition ease-out duration-200"
        x-transition:enter-start="opacity-0"
        x-transition:enter-end="opacity-100"
        x-transition:leave="transition ease-in duration-150"
        x-transition:leave-start="opacity-100"
        x-transition:leave-end="opacity-0"
        class="fixed inset-0 z-50 flex items-center justify-center"
        style="background: rgba(0,0,0,0.4);"
        @click.self="showSaveModal = false">
        <div
          x-show="showSaveModal"
          x-transition:enter="transition ease-out duration-200"
          x-transition:enter-start="opacity-0 scale-95"
          x-transition:enter-end="opacity-100 scale-100"
          x-transition:leave="transition ease-in duration-150"
          x-transition:leave-start="opacity-100 scale-100"
          x-transition:leave-end="opacity-0 scale-95"
          class="save-modal-card">

          <div class="text-center">
            <div class="save-modal-checkmark">
              <svg
                class="w-6 h-6"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
                stroke-width="2.5">
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  d="M5 13l4 4L19 7" />
              </svg>
            </div>
            <p class="mt-4 mb-1 text-gray-700 text-balance">Together, we just added visual editing to a website.</p>
            <p class="mb-6 font-semibold text-gray-900">Imagine what you could do with yours.</p>
            <a
              href="https://cloudcannon.com/free-trial/"
              target="_blank"
              rel="noopener"
              class="inline-block px-6 py-2.5 text-sm text-white font-semibold rounded-md transition-colors hover:opacity-90"
              style="background-color: #034AD8;">Sign up for a free trial</a>
            <a
              href="https://cloudcannon.com/documentation/"
              target="_blank"
              rel="noopener"
              class="block mt-3 text-sm font-medium text-gray-500 transition-colors hover:text-gray-700">Read the docs &rarr;</a>
          </div>

          <div x-show="!allTutorialsComplete" class="save-modal-divider"></div>
          <div x-show="!allTutorialsComplete" class="save-modal-tracks">
            <template x-for="track in tutorialTracks" :key="track.id">
              <button
                class="save-modal-track-row"
                :class="track.complete ? 'is-complete' : ''"
                :disabled="track.complete"
                @click="!track.complete && startTutorialFromModal(track)">
                <span class="save-modal-track-icon">
                  <template x-if="track.complete">
                    <svg
                      class="w-3 h-3"
                      xmlns="http://www.w3.org/2000/svg"
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
                      stroke-width="3">
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        d="M5 13l4 4L19 7" />
                    </svg>
                  </template>
                </span>
                <span class="save-modal-track-label" x-text="track.label"></span>
                <span class="save-modal-track-count" x-text="track.stepsCompleted + '/' + track.totalSteps"></span>
                <svg
                  x-show="!track.complete"
                  class="w-3 h-3 ml-auto opacity-40"
                  xmlns="http://www.w3.org/2000/svg"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor">
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2.5"
                    d="M9 5l7 7-7 7" />
                </svg>
              </button>
            </template>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    document.addEventListener('alpine:init', () => {
      Alpine.data('sourceEditorManager', () => ({
        hasChanges: false,
        showSaveModal: false,
        lastEditedProp: null,
        flashTimeout: null,
        currentSite: 'jetstream',

        activeTutorial: 'heading',
        tutorialStep: 0,
        lastAdvancedStep: null,
        advanceTimeout: null,
        headingTutorialComplete: false,
        imageTutorialComplete: false,
        dataTutorialComplete: false,
        highlightTarget: null,
        highlightTimeout: null,
        lastCompletedTutorial: null,
        codeScrollTop: 0,

        blogTitle: 'Start building with Jetstream today',
        blogBody: 'Deploy your first project in <strong>under five minutes</strong>. Jetstream gives your team the infrastructure to ship faster, scale further, and sleep better at night. With edge-optimized delivery and <em>sub-50ms response times</em> worldwide, your users get the performance they deserve.',

        headingMain: 'Introducing',
        headingHighlight: 'Jetstream',
        description: 'A new Astro template by CloudCannon, built with our <a href="#">Astro Component Starter</a> for top performance, true visual editing, and an ideal developer experience.',

        heroImageSrc: '/dashboard.svg',
        heroImageAlt: 'Dashboard',
        heroImagePreviewSrc: '/demo-sites/jetstream/images/hero-1.svg',
        showImagePicker: false,
        showImageSourceMenu: false,
        showGallery: false,

        sites: {
          jetstream: {
            label: 'Jetstream',
            defaults: {
              headingMain: 'Introducing',
              headingHighlight: 'Jetstream',
              description: 'A new Astro template by CloudCannon, built with our <a href="#">Astro Component Starter</a> for top performance, true visual editing, and an ideal developer experience.',
            },
            regions: [
              { key: 'heading-main', prop: 'headingMain', tag: 'span' },
              { key: 'heading-highlight', prop: 'headingHighlight', tag: 'span', className: 'highlight' },
            ],
            sourcePath: '/index.astro',
            sidebarFilename: 'index.astro',
            imageDefaults: { src: '/dashboard.svg', alt: 'Dashboard', previewSrc: '/demo-sites/jetstream/images/hero-1.svg' },
            presetImages: [
              { src: '/dashboard.svg', previewSrc: '/demo-sites/jetstream/images/hero-1.svg' },
              { src: '/hero-2.svg', previewSrc: '/demo-sites/jetstream/images/hero-2.svg' },
              { src: '/hero-3.svg', previewSrc: '/demo-sites/jetstream/images/hero-3.svg' },
              { src: '/hero-4.svg', previewSrc: '/demo-sites/jetstream/images/hero-4.svg' },
              { src: '/hero-5.svg', previewSrc: '/demo-sites/jetstream/images/hero-5.svg' },
            ],
            imageKey: 'hero-image',
            blogDefaults: {
              blogTitle: 'Start building with Jetstream today',
              blogBody: 'Deploy your first project in <strong>under five minutes</strong>. Jetstream gives your team the infrastructure to ship faster, scale further, and sleep better at night. With edge-optimized delivery and <em>sub-50ms response times</em> worldwide, your users get the performance they deserve.',
            },
            blogSidebarTopFilename: 'pages/blog/[...slug].astro',
            blogSidebarBottomFilename: 'getting-started.md',
            blogDate: 'March 15, 2026',
            blogAuthor: 'Alex Chen',
          },
        },

        get site() { return this.sites[this.currentSite]; },
        get regions() { return this.site.regions; },
        get sourcePath() { return this.site.sourcePath; },
        get sidebarFilename() { return this.site.sidebarFilename; },

        get headingSetupLevel() {
          if (this.headingTutorialComplete) return 4;
          if (this.activeTutorial === 'heading') return this.tutorialStep;
          return 0;
        },

        get imageSetupLevel() {
          if (this.imageTutorialComplete) return 4;
          if (this.activeTutorial === 'image') return this.tutorialStep;
          return 0;
        },

        get headingEditable() { return this.headingSetupLevel >= 3; },
        get imageEditable() { return this.imageSetupLevel >= 3; },

        get dataSetupLevel() {
          if (this.dataTutorialComplete) return 4;
          if (this.activeTutorial === 'data') {
            return this.tutorialStep <= 3 ? this.tutorialStep : this.tutorialStep - 1;
          }
          return 0;
        },

        get maxTutorialStep() {
          if (this.activeTutorial === 'data') return 5;
          return 4;
        },

        get titleEditable() { return this.dataSetupLevel >= 2; },
        get bodyEditable() { return this.dataSetupLevel >= 3; },

        get showDataView() {
          if (this.activeTutorial === 'data' || this.activeTutorial === 'image') return true;
          if (this.activeTutorial === null && (this.lastCompletedTutorial === 'data' || this.lastCompletedTutorial === 'image')) return true;
          return false;
        },

        switchSite(name) {
          if (name === this.currentSite) return;
          this.currentSite = name;
          this.activeTutorial = 'heading';
          this.tutorialStep = 0;
          this.lastAdvancedStep = null;
          this.headingTutorialComplete = false;
          this.imageTutorialComplete = false;
          this.dataTutorialComplete = false;
          this.lastCompletedTutorial = null;
          this.hasChanges = false;
          this.lastEditedProp = null;
          this.showImagePicker = false;
          this.showImageSourceMenu = false;
          this.showGallery = false;
          clearTimeout(this.flashTimeout);
          clearTimeout(this.advanceTimeout);
          const d = this.sites[name].defaults;
          this.headingMain = d.headingMain;
          this.headingHighlight = d.headingHighlight;
          this.description = d.description;
          const img = this.sites[name].imageDefaults;
          this.heroImageSrc = img.src;
          this.heroImageAlt = img.alt;
          this.heroImagePreviewSrc = img.previewSrc;
          const blog = this.sites[name].blogDefaults;
          this.blogTitle = blog.blogTitle;
          this.blogBody = blog.blogBody;
          this.codeScrollTop = 0;
          this.$nextTick(() => {
            const el = this.$refs.codeScroll;
            if (el) el.scrollTo({ top: 0 });
          });
        },

        headingTooltipSteps: [
          { text: 'This Astro page has hard-coded content. Let\u2019s make the heading editable with Source Editable Regions.', button: 'Start' },
          { text: 'This attribute tells CloudCannon that this element\u2019s content can be edited directly in the source.', button: 'Next' },
          { text: 'This tells CloudCannon which source file the content lives in.', button: 'Next' },
          { text: 'This unique key identifies the editable region. Each region in a file needs its own key.', button: 'Next' },
          { text: 'Your heading is set up! Try editing it on the right \u2014 the source updates in real time.', button: 'Try it' },
        ],

        imageTooltipSteps: [
          { text: 'This blog post has a featured image. Let\u2019s make it editable with Image Editables.', button: 'Start' },
          { text: 'This tells CloudCannon that this element is an editable image region.', button: 'Next' },
          { text: 'This maps the image source to the featured_image field in the markdown frontmatter below.', button: 'Next' },
          { text: 'This maps the alt text to the featured_image_alt field in the frontmatter.', button: 'Next' },
          { text: 'Your image is set up! Click it on the right to pick a new one.', button: 'Try it' },
        ],

        dataTooltipSteps: [
          { text: 'This blog post renders structured data from markdown frontmatter. Let\u2019s make the title editable with Data Editables.', button: 'Start' },
          { text: 'This tells CloudCannon that this element shows editable text from structured data.', button: 'Next' },
          { text: 'This maps the element to the title field in the markdown frontmatter below.', button: 'Next' },
          { text: 'If there is no appropriate DOM element, you can wrap elements in the equivalent web component, which will accept HTML attributes. Here <editable-text> is used to wrap the body content of the markdown file.', button: 'Next' },
          { text: 'The @content prop tells CloudCannon this is the body content of the markdown file.', button: 'Next' },
          { text: 'Your blog post is set up! Try editing the title and content on the right.', button: 'Try it' },
        ],

        get currentTooltipSteps() {
          if (this.activeTutorial === 'data') return this.dataTooltipSteps;
          if (this.activeTutorial === 'image') return this.imageTooltipSteps;
          return this.headingTooltipSteps;
        },

        regionLines(region, indent) {
          const s = this.headingSetupLevel;
          const adv = this.activeTutorial === 'heading' ? this.lastAdvancedStep : null;
          const val = this[region.prop];
          const pad = ' '.repeat(indent);
          const attrPad = ' '.repeat(indent + 2);
          const lines = [];

          if (s === 0) {
            const cls = region.className ? ` class="${region.className}"` : '';
            lines.push({ text: pad + '<' + region.tag + cls + '>' + val + '</' + region.tag + '>', isHeading: true });
            return lines;
          }

          lines.push({ text: pad + '<' + region.tag, isHeading: true });

          if (region.className) {
            lines.push({ text: attrPad + 'class="' + region.className + '"', isHeading: true });
          }

          if (s === 1) {
            lines.push({ text: attrPad + 'data-editable="source">', isNew: adv === 1, isHeading: true });
          } else {
            lines.push({ text: attrPad + 'data-editable="source"', isHeading: true });
          }

          if (s >= 2 && s === 2) {
            lines.push({ text: attrPad + 'data-path="' + this.sourcePath + '">', isNew: adv === 2, isHeading: true });
          } else if (s >= 2) {
            lines.push({ text: attrPad + 'data-path="' + this.sourcePath + '"', isHeading: true });
          }

          if (s >= 3) {
            lines.push({ text: attrPad + 'data-key="' + region.key + '">', isNew: adv === 3, isHeading: true });
          }

          lines.push(
            { text: attrPad + val, prop: region.prop, isHeading: true },
            { text: pad + '</' + region.tag + '>', isHeading: true },
          );

          return lines;
        },

        get sourceLines() {
          return this.jetstreamSourceLines;
        },

        get jetstreamSourceLines() {
          const lines = [];

          lines.push(
            { text: '---' },
            { text: 'import Layout from "../layouts/BaseLayout.astro"' },
            { text: '---' },
            { text: '' },
            { text: '<Layout title="home">' },
            { text: '  <section class="hero">' },
            { text: '    <h1>' },
          );

          for (const region of this.regions) {
            lines.push(...this.regionLines(region, 6));
          }

          lines.push(
            { text: '    </h1>' },
            { text: '    <p>A new Astro template by CloudCannon...</p>' },
            { text: '    <a href="/get-started" class="button">Deploy Jetstream</a>' },
            { text: '    <img src="/dashboard.svg" alt="Dashboard" />' },
          );

          lines.push(
            { text: '  </section>' },
            { text: '' },
            { text: '  <!-- ... Rest of page here -->' },
            { text: '' },
            { text: '</Layout>' },
          );

          return lines;
        },

        get dataPageSourceLines() {
          const s = this.dataSetupLevel;
          const imgS = this.imageSetupLevel;
          const adv = this.activeTutorial === 'data' ? this.lastAdvancedStep : null;
          const imgAdv = this.activeTutorial === 'image' ? this.lastAdvancedStep : null;
          const lines = [];

          lines.push(
            { text: '---' },
            { text: 'import Layout from "../layouts/Layout.astro"' },
            { text: 'import { getEntry } from "astro:content"' },
            { text: '' },
            { text: 'const post = await getEntry("blog", Astro.params.slug)' },
            { text: 'const { Content } = await post.render()' },
            { text: 'const { title, date, author, featured_image, featured_image_alt } = post.data' },
            { text: '---' },
            { text: '' },
            { text: '<Layout>' },
            { text: '  <article>' },
          );

          if (s === 0) {
            lines.push({ text: '    <h1>{title}</h1>', isData: true });
          } else {
            lines.push({ text: '    <h1', isData: true });
            if (s === 1) {
              lines.push({ text: '      data-editable="text">', isNew: adv === 1, isData: true });
            } else {
              lines.push({ text: '      data-editable="text"', isData: true });
              lines.push({ text: '      data-prop="title">', isNew: adv === 2, isData: true, prop: 'blogTitle' });
            }
            lines.push(
              { text: '      {title}', isData: true },
              { text: '    </h1>', isData: true },
            );
          }

          lines.push({ text: '    <p class="meta">{date} · {author}</p>' });

          if (imgS === 0) {
            lines.push({ text: '    <img src={featured_image} alt={featured_image_alt} />', isImage: true });
          } else {
            lines.push({ text: '    <img', isImage: true });
            lines.push({ text: '      src={featured_image}', isImage: true, prop: 'heroImageSrc' });
            lines.push({ text: '      alt={featured_image_alt}', isImage: true, prop: 'heroImageAlt' });
            if (imgS === 1) {
              lines.push({ text: '      data-editable="image" />', isNew: imgAdv === 1, isImage: true });
            } else {
              lines.push({ text: '      data-editable="image"', isImage: true });
            }
            if (imgS >= 2 && imgS === 2) {
              lines.push({ text: '      data-prop-src="featured_image" />', isNew: imgAdv === 2, isImage: true });
            } else if (imgS >= 2) {
              lines.push({ text: '      data-prop-src="featured_image"', isImage: true });
            }
            if (imgS >= 3) {
              lines.push({ text: '      data-prop-alt="featured_image_alt" />', isNew: imgAdv === 3, isImage: true });
            }
          }

          lines.push({ text: '    <div class="max-w-4xl mx-auto prose lg:prose-xl">' });

          if (s >= 3) {
            lines.push(
              { text: '      <editable-text data-prop="@content">', isNew: adv === 3, isBody: true },
              { text: '        <Content />', isBody: true },
              { text: '      </editable-text>', isNew: adv === 3, isBody: true },
            );
          } else {
            lines.push({ text: '      <Content />', isBody: true });
          }

          lines.push(
            { text: '    </div>' },
            { text: '  </article>' },
            { text: '</Layout>' },
          );

          return lines;
        },

        htmlToMarkdown(html) {
          return html
            .replace(/<\/?(strong|b)>/gi, '**')
            .replace(/<\/?(em|i)>/gi, '*')
            .replace(/<br\s*\/?>/gi, ' ')
            .replace(/<[^>]+>/g, '');
        },

        get dataMarkdownLines() {
          const site = this.site;
          const s = this.dataSetupLevel;
          const imgS = this.imageSetupLevel;
          const lines = [];

          lines.push(
            { text: '---', isFrontmatter: true },
            { text: 'title: ' + this.blogTitle, isFrontmatter: true, prop: 'blogTitle', highlightAt: s >= 2 },
            { text: 'date: ' + site.blogDate, isFrontmatter: true },
            { text: 'author: ' + site.blogAuthor, isFrontmatter: true },
            { text: 'featured_image: ' + this.heroImageSrc, isFrontmatter: true, prop: 'heroImageSrc', imageHighlightAt: imgS >= 2 },
            { text: 'featured_image_alt: ' + this.heroImageAlt, isFrontmatter: true, prop: 'heroImageAlt', imageHighlightAt: imgS >= 3 },
            { text: 'draft: false', isFrontmatter: true },
            { text: '---', isFrontmatter: true },
            { text: '' },
          );

          const md = this.htmlToMarkdown(this.blogBody);
          const bodyLines = md.split(/(?<=\.)\s+/);
          for (const line of bodyLines) {
            lines.push({ text: line, isMarkdownBody: true });
          }

          return lines;
        },

        nextStep() {
          if (this.tutorialStep > this.maxTutorialStep) return;

          if (this.tutorialStep === this.maxTutorialStep) {
            this.lastCompletedTutorial = this.activeTutorial;
            if (this.activeTutorial === 'heading') {
              this.headingTutorialComplete = true;
              this.highlightTarget = 'heading';
            } else if (this.activeTutorial === 'image') {
              this.imageTutorialComplete = true;
              this.highlightTarget = 'image';
            } else if (this.activeTutorial === 'data') {
              this.dataTutorialComplete = true;
              this.highlightTarget = 'data';
            }
            this.activeTutorial = null;
            clearTimeout(this.highlightTimeout);
            this.highlightTimeout = setTimeout(() => { this.highlightTarget = null; }, 4000);
            return;
          }

          this.tutorialStep++;

          if (this.tutorialStep >= 1 && this.tutorialStep <= 3) {
            this.lastAdvancedStep = this.tutorialStep;
            clearTimeout(this.advanceTimeout);
            this.advanceTimeout = setTimeout(() => {
              this.lastAdvancedStep = null;
            }, 3000);
          }

          this.$nextTick(() => this.scrollToActiveLine());
        },

        startImageTutorial() {
          this.activeTutorial = 'image';
          this.tutorialStep = 0;
          this.lastAdvancedStep = null;
          this.codeScrollTop = 0;
          this.$nextTick(() => {
            const el = this.$refs.dataCodeScrollTop;
            if (el) el.scrollTo({ top: 0 });
            this.scrollToActiveLine(true);
          });
        },

        startDataTutorial() {
          this.activeTutorial = 'data';
          this.tutorialStep = 0;
          this.lastAdvancedStep = null;
          this.codeScrollTop = 0;
          this.$nextTick(() => {
            const el = this.$refs.dataCodeScrollTop;
            if (el) el.scrollTo({ top: 0 });
            this.scrollToActiveLine(true);
          });
        },

        rawTooltipTop() {
          const lineH = 24;
          const tabH = 32;

          if (this.activeTutorial === 'data') {
            const dataTabH = 36;
            const imgS = this.imageSetupLevel;
            const imgLines = imgS === 0 ? 1 : imgS <= 1 ? 4 : imgS <= 2 ? 5 : 6;
            switch (this.tutorialStep) {
              case 0: return dataTabH + 11 * lineH;
              case 1: return dataTabH + 12 * lineH;
              case 2: return dataTabH + 13 * lineH;
              case 3: return dataTabH + (18 + imgLines) * lineH;
              case 4: return dataTabH + (18 + imgLines) * lineH;
              case 5: return dataTabH + 11 * lineH;
              default: return 0;
            }
          }

          if (this.activeTutorial === 'image') {
            const dataTabH = 36;
            const ds = this.dataSetupLevel;
            const h1Lines = ds === 0 ? 1 : ds <= 1 ? 4 : 5;
            const imgLine = 11 + h1Lines + 1;
            switch (this.tutorialStep) {
              case 0: return dataTabH + imgLine * lineH;
              case 1: return dataTabH + (imgLine + 3) * lineH;
              case 2: return dataTabH + (imgLine + 4) * lineH;
              case 3: return dataTabH + (imgLine + 5) * lineH;
              case 4: return dataTabH + imgLine * lineH;
              default: return 0;
            }
          }

          switch (this.tutorialStep) {
            case 0: return tabH + 6 * lineH;
            case 1: return tabH + 8 * lineH;
            case 2: return tabH + 9 * lineH;
            case 3: return tabH + 10 * lineH;
            case 4: return tabH + 6 * lineH;
            default: return 0;
          }
        },

        tooltipTop() {
          const raw = this.rawTooltipTop();
          const visual = raw - this.codeScrollTop;
          return Math.max(60, Math.min(visual, 540));
        },

        scrollToActiveLine(force) {
          const useSplitView = this.activeTutorial === 'data' || this.activeTutorial === 'image';
          const el = useSplitView
            ? this.$refs.dataCodeScrollTop
            : this.$refs.codeScroll;
          if (!el) return;
          const rawTop = this.rawTooltipTop();
          const offsetH = useSplitView ? 36 : 32;
          const lineY = rawTop - offsetH;
          const viewH = el.clientHeight;
          const currentScroll = el.scrollTop;
          const padding = 100;
          const isVisible = lineY >= currentScroll + padding
                         && lineY <= currentScroll + viewH - padding;

          if (force || !isVisible) {
            const targetScroll = Math.max(0, lineY - viewH * 0.4);
            el.scrollTo({ top: targetScroll, behavior: 'smooth' });
            this.codeScrollTop = targetScroll;
          } else {
            this.codeScrollTop = currentScroll;
          }
        },

        applyPresetImage(preset) {
          this.heroImageSrc = preset.src;
          this.heroImagePreviewSrc = preset.previewSrc;
          this.showGallery = false;
          this.showImageSourceMenu = false;
          this.onEdit('heroImageSrc');
        },

        applyPathInput() {
          const val = this.heroImageSrc.trim();
          if (!val) return;
          if (val.startsWith('http://') || val.startsWith('https://')) {
            this.heroImagePreviewSrc = val;
          } else {
            const match = this.site.presetImages.find(p => p.src === val);
            if (match) this.heroImagePreviewSrc = match.previewSrc;
          }
          this.onEdit('heroImageSrc');
        },

        handleImageUpload(event) {
          const file = event.target.files?.[0];
          if (!file) return;
          this.heroImagePreviewSrc = URL.createObjectURL(file);
          this.heroImageSrc = '/uploads/' + file.name;
          this.showImageSourceMenu = false;
          this.onEdit('heroImageSrc');
        },

        onEdit(prop) {
          this.hasChanges = true;
          this.lastEditedProp = prop;
          this.highlightTarget = null;
          clearTimeout(this.highlightTimeout);
          clearTimeout(this.flashTimeout);
          this.flashTimeout = setTimeout(() => {
            this.lastEditedProp = null;
          }, 1500);
        },

        get allTutorialsComplete() {
          return this.headingTutorialComplete && this.imageTutorialComplete && this.dataTutorialComplete;
        },

        get tutorialTracks() {
          return [
            {
              id: 'heading',
              label: 'Configure text editing',
              complete: this.headingTutorialComplete,
              stepsCompleted: this.headingTutorialComplete ? 5 : (this.activeTutorial === 'heading' ? this.tutorialStep : 0),
              totalSteps: 5,
            },
            {
              id: 'data',
              label: 'Configure structured data',
              complete: this.dataTutorialComplete,
              stepsCompleted: this.dataTutorialComplete ? 6 : (this.activeTutorial === 'data' ? this.tutorialStep : 0),
              totalSteps: 6,
            },
            {
              id: 'image',
              label: 'Configure image editing',
              complete: this.imageTutorialComplete,
              stepsCompleted: this.imageTutorialComplete ? 5 : (this.activeTutorial === 'image' ? this.tutorialStep : 0),
              totalSteps: 5,
            },
          ];
        },

        startTutorialFromModal(track) {
          this.showSaveModal = false;
          if (track.id === this.activeTutorial) return;
          if (track.id === 'heading') {
            this.activeTutorial = 'heading';
            this.tutorialStep = 0;
          } else if (track.id === 'image') {
            this.startImageTutorial();
          } else if (track.id === 'data') {
            this.startDataTutorial();
          }
        },
      }));
    });
  </script>

  <style>
    [x-cloak] {
      display: none !important;
    }

    .editable-region {
      outline: 2px solid transparent;
      outline-offset: 4px;
      transition: outline-color 0.15s ease;
      cursor: pointer;
    }

    .editable-region:hover,
    .editable-region:focus {
      outline-color: #FCBD01;
    }

    .editable-region:focus {
      outline-style: solid;
    }

    .editable-region-image {
      outline: 2px solid transparent;
      outline-offset: 4px;
      transition: outline-color 0.15s ease;
      cursor: pointer;
      border-radius: 4px;
    }

    .editable-region-image:hover {
      outline-color: #FCBD01;
    }

    .editable-region-highlight {
      animation: region-pulse 0.4s ease-in-out 3;
      outline-color: #FCBD01;
    }

    @keyframes region-pulse {
      0,
      100% {
        outline-color: #FCBD01;
      }
      50% {
        outline-color: transparent;
      }
    }

    .blog-body-toolbar {
      display: flex;
      gap: 2px;
      max-width: 380px;
      margin: 0 auto;
      padding: 4px 1rem;
      border-bottom: 1px solid #e2e2e2;
    }

    .blog-body-toolbar button {
      width: 24px;
      height: 22px;
      display: flex;
      align-items: center;
      justify-content: center;
      border: none;
      border-radius: 3px;
      background: transparent;
      cursor: pointer;
      font-size: 0.55rem;
      color: #444;
      line-height: 1;
    }

    .blog-body-toolbar button:hover {
      background: #e8e8e8;
    }

    .save-btn {
      padding: 5px 14px;
      border: none;
      cursor: pointer;
    }

    .save-btn-active {
      background: #E8734A;
      color: #fff;
      box-shadow: 0 1px 4px rgba(232, 115, 74, 0.3);
      animation: save-glow 2s ease-in-out infinite;
    }

    .save-btn-active:hover {
      background: #d4623c;
    }

    .save-btn-inactive {
      background: #f3f4f6;
      color: #9ca3af;
      border: 1px solid #e5e7eb;
    }

    .save-btn-badge {
      position: absolute;
      top: -6px;
      right: -6px;
      width: 16px;
      height: 16px;
      border-radius: 50%;
      background: #dc2626;
      color: #fff;
      font-size: 10px;
      font-weight: 700;
      display: flex;
      align-items: center;
      justify-content: center;
      line-height: 1;
    }

    @keyframes save-glow {
      0,
      100% {
        box-shadow: 0 1px 4px rgba(232, 115, 74, 0.3);
      }
      50% {
        box-shadow: 0 0 0 5px rgba(232, 115, 74, 0);
      }
    }

    .tutorial-tooltip {
      position: absolute;
      z-index: 20;
      left: calc(50% + 12px);
      width: 260px;
      padding: 14px 16px;
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.18);
      transition: top 0.3s ease;
    }

    .tutorial-tooltip::before {
      content: '';
      position: absolute;
      left: -8px;
      top: 18px;
      width: 0;
      height: 0;
      border-top: 8px solid transparent;
      border-bottom: 8px solid transparent;
      border-right: 8px solid #fff;
    }

    .image-picker {
      position: absolute;
      z-index: 30;
      right: 24px;
      top: 50%;
      transform: translateY(-50%);
      width: 340px;
      padding: 16px 18px;
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.18);
    }

    .image-picker-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 14px;
    }

    .image-picker-path-row {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .image-picker-path-input {
      flex: 1;
      min-width: 0;
      padding: 6px 10px;
      font-size: 13px;
      border: 1px solid #d1d5db;
      border-radius: 6px;
      outline: none;
      transition: border-color 0.15s ease
      , box-shadow 0.15s ease;
    }

    .image-picker-path-input:focus {
      border-color: #93c5fd;
      box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2);
    }

    .image-picker-path-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 32px;
      height: 32px;
      padding: 0;
      border: 1px solid #d1d5db;
      border-radius: 6px;
      background: #fff;
      color: #6b7280;
      cursor: pointer;
      flex-shrink: 0;
      transition: background-color 0.15s ease
      , border-color 0.15s ease;
    }

    .image-picker-path-btn:hover {
      background: #f3f4f6;
      border-color: #9ca3af;
    }

    .image-picker-source-menu {
      position: absolute;
      top: 100%;
      right: 0;
      margin-top: 4px;
      width: 220px;
      background: #fff;
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
      z-index: 40;
      overflow: hidden;
    }

    .image-picker-source-option {
      display: flex;
      align-items: center;
      gap: 10px;
      width: 100%;
      padding: 10px 14px;
      font-size: 13px;
      color: #374151;
      background: none;
      border: none;
      cursor: pointer;
      transition: background-color 0.1s ease;
      text-align: left;
    }

    .image-picker-source-option:hover {
      background: #f3f4f6;
    }

    .image-picker-preview {
      margin-bottom: 14px;
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      overflow: hidden;
      background: #f8fafc;
    }

    .image-picker-preview img {
      display: block;
      width: 100%;
      max-height: 180px;
      object-fit: contain;
    }

    .image-picker-alt-input {
      width: 100%;
      padding: 8px 10px;
      font-size: 13px;
      line-height: 1.5;
      border: 1px solid #d1d5db;
      border-radius: 6px;
      outline: none;
      resize: vertical;
      transition: border-color 0.15s ease
      , box-shadow 0.15s ease;
    }

    .image-picker-alt-input:focus {
      border-color: #93c5fd;
      box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2);
    }

    .image-picker-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 8px;
    }

    .image-picker-thumb {
      aspect-ratio: 4 / 3;
      border-radius: 6px;
      overflow: hidden;
      border: 2px solid transparent;
      cursor: pointer;
      padding: 0;
      background: #f1f5f9;
      transition: border-color 0.15s ease
      , box-shadow 0.15s ease;
    }

    .image-picker-thumb:hover {
      border-color: #cbd5e1;
    }

    .image-picker-thumb.selected {
      border-color: #034AD8;
      box-shadow: 0 0 0 1px #034AD8;
    }

    .image-picker-thumb img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .save-modal-card {
      width: 100%;
      max-width: 420px;
      margin: 0 16px;
      padding: 32px;
      background: #fff;
      border-radius: 10px;
      border-top: 4px solid #034AD8;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
    }

    .save-modal-checkmark {
      width: 48px;
      height: 48px;
      margin: 0 auto;
      border-radius: 50%;
      background: #d1fae5;
      color: #16a34a;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .save-modal-divider {
      height: 1px;
      background: #e5e7eb;
      margin: 20px 0 16px;
    }

    .save-modal-tracks {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .save-modal-track-row {
      display: flex;
      align-items: center;
      gap: 8px;
      width: 100%;
      padding: 6px 8px;
      border: none;
      border-radius: 6px;
      background: transparent;
      font-size: 13px;
      color: #6b7280;
      cursor: pointer;
      transition: background 0.15s ease;
      text-align: left;
    }

    .save-modal-track-row:not(.is-complete):hover {
      background: #f3f4f6;
    }

    .save-modal-track-row.is-complete {
      cursor: default;
      color: #16a34a;
    }

    .save-modal-track-icon {
      width: 18px;
      height: 18px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      border: 2px solid #d1d5db;
    }

    .save-modal-track-row.is-complete .save-modal-track-icon {
      background: #d1fae5;
      border-color: #16a34a;
      color: #16a34a;
    }

    .save-modal-track-label {
      flex: 1;
    }

    .save-modal-track-count {
      font-size: 11px;
      font-weight: 600;
      padding: 1px 6px;
      border-radius: 999px;
      background: #f3f4f6;
    }

    .save-modal-track-row.is-complete .save-modal-track-count {
      background: #d1fae5;
      color: #16a34a;
    }
  </style>

</section>